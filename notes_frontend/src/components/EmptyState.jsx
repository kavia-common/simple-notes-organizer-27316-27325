import React from 'react';

/**
 * PUBLIC_INTERFACE
 * EmptyState: Shown when there are no notes or none selected.
 */
export default function EmptyState(){
  return <div className="empty card">No notes yet. Click "New Note" to create your first note.</div>;
}
